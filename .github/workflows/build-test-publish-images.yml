name: Build, Test and Publish PT-LIMS Images

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read
  pull-requests: read

concurrency:
  group: ptl-images-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  DOTNET_VERSION: 10.0.x
  AWS_REGION: ${{ vars.AWS_REGION }}
  AWS_ROLE_ARN: ${{ vars.AWS_OIDC_ROLE_ARN }}

jobs:
  changes:
    name: Detect affected components
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      components: ${{ steps.filter.outputs.changes }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Detect changed paths
        id: filter
        uses: dorny/paths-filter@v4.0.3
        with:
          filters: |
            api:
              - 'src/PTL.Api/**'
              - 'tests/PTL.Api.Tests/**'
              - 'PTL.slnx'
              - 'Directory.Build.*'
              - 'Directory.Packages.props'
              - 'NuGet.config'
              - 'global.json'
              - '.github/workflows/build-test-publish-images.yml'
            internalweb:
              - 'src/PTL.InternalWeb/**'
              - 'src/PTL.ApiClient/**'
              - 'tests/PTL.InternalWeb.Tests/**'
              - 'tests/PTL.ApiClient.Tests/**'
              - 'PTL.slnx'
              - 'Directory.Build.*'
              - 'Directory.Packages.props'
              - 'NuGet.config'
              - 'global.json'
              - '.github/workflows/build-test-publish-images.yml'
            externalweb:
              - 'src/PTL.ExternalWeb/**'
              - 'src/PTL.ApiClient/**'
              - 'tests/PTL.ExternalWeb.Tests/**'
              - 'tests/PTL.ApiClient.Tests/**'
              - 'PTL.slnx'
              - 'Directory.Build.*'
              - 'Directory.Packages.props'
              - 'NuGet.config'
              - 'global.json'
              - '.github/workflows/build-test-publish-images.yml'

  quality:
    name: Format, build and test solution
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7.0.1
        with:
          persist-credentials: false

      - name: Set up .NET
        uses: actions/setup-dotnet@v6.0.0
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore solution
        run: dotnet restore PTL.slnx

      - name: Verify formatting
        run: dotnet format PTL.slnx --verify-no-changes --no-restore

      - name: Build solution
        run: dotnet build PTL.slnx --configuration Release --no-restore --warnaserror

      - name: Test solution
        run: >-
          dotnet test PTL.slnx
          --configuration Release
          --no-build
          --no-restore
          --logger "trx;LogFilePrefix=ptl-tests"
          --results-directory "${{ runner.temp }}/test-results"

  sonarcloud:
    name: SonarCloud analysis
    if: vars.SONAR_ENABLED == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout full history
        uses: actions/checkout@v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Set up .NET
        uses: actions/setup-dotnet@v6.0.0
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Set up Java
        uses: actions/setup-java@v6.0.0
        with:
          distribution: temurin
          java-version: "17"

      - name: Restore repository tools
        run: dotnet tool restore

      - name: Analyse solution
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          set -euo pipefail
          test -n "${SONAR_TOKEN}"
          dotnet sonarscanner begin \
            /k:"${{ vars.SONAR_PROJECT_KEY }}" \
            /o:"${{ vars.SONAR_ORGANIZATION }}" \
            /d:sonar.token="${SONAR_TOKEN}" \
            /d:sonar.host.url="https://sonarcloud.io" \
            /d:sonar.qualitygate.wait=true
          dotnet restore PTL.slnx
          dotnet build PTL.slnx --configuration Release --no-restore
          dotnet sonarscanner end /d:sonar.token="${SONAR_TOKEN}"

  container-validation:
    name: Validate ${{ matrix.component }} container
    needs: [changes, quality]
    if: needs.changes.outputs.components != '[]'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        component: ${{ fromJSON(needs.changes.outputs.components) }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7.0.1
        with:
          persist-credentials: false

      - name: Resolve component configuration
        id: component
        shell: bash
        run: |
          set -euo pipefail
          case "${{ matrix.component }}" in
            api)
              echo "dockerfile=src/PTL.Api/Dockerfile" >> "${GITHUB_OUTPUT}"
              ;;
            internalweb)
              echo "dockerfile=src/PTL.InternalWeb/Dockerfile" >> "${GITHUB_OUTPUT}"
              ;;
            externalweb)
              echo "dockerfile=src/PTL.ExternalWeb/Dockerfile" >> "${GITHUB_OUTPUT}"
              ;;
            *)
              echo "::error::Unsupported component: ${{ matrix.component }}"
              exit 1
              ;;
          esac

      - name: Build container without publishing
        shell: bash
        run: |
          set -euo pipefail
          docker buildx build \
            --file "${{ steps.component.outputs.dockerfile }}" \
            --tag "ptl-${{ matrix.component }}:${GITHUB_SHA}" \
            --load \
            .

  publish:
    name: Publish ${{ matrix.component }} image
    needs: [changes, quality, sonarcloud, container-validation]
    if: >-
      always() &&
      github.event_name == 'push' &&
      github.ref == 'refs/heads/main' &&
      needs.changes.result == 'success' &&
      needs.quality.result == 'success' &&
      (vars.SONAR_ENABLED != 'true' || needs.sonarcloud.result == 'success') &&
      needs.container-validation.result == 'success' &&
      needs.changes.outputs.components != '[]'
    runs-on: ubuntu-latest
    environment: ecr-production
    timeout-minutes: 25
    permissions:
      contents: read
      id-token: write
    strategy:
      fail-fast: false
      matrix:
        component: ${{ fromJSON(needs.changes.outputs.components) }}
    env:
      AWS_ROLE_ARN: "arn:aws:iam::783559591987:role/github_actions_pipeline_ecr_role"
      ECR_API_REPOSITORY: apha/ptlapi
      ECR_INTERNAL_REPOSITORY: apha/ptlinternal
      ECR_EXTERNAL_REPOSITORY: apha/ptlexternal
      EXPECTED_AWS_ACCOUNT_ID: "783559591987"
      AWS_REGION: "eu-west-2"
      AWS_ACCOUNT_ID: "783559591987"
      
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7.0.1
        with:
          persist-credentials: false

      - name: Resolve and validate publishing configuration
        id: component
        shell: bash
        run: |
          set -euo pipefail

          test -n "${AWS_ROLE_ARN}" || {
            echo "::error::Environment variable AWS_OIDC_ROLE_ARN is not configured."
            exit 1
          }

          case "${{ matrix.component }}" in
            api)
              DOCKERFILE="src/PTL.Api/Dockerfile"
              ECR_REPOSITORY="${ECR_API_REPOSITORY}"
              ;;
            internalweb)
              DOCKERFILE="src/PTL.InternalWeb/Dockerfile"
              ECR_REPOSITORY="${ECR_INTERNAL_REPOSITORY}"
              ;;
            externalweb)
              DOCKERFILE="src/PTL.ExternalWeb/Dockerfile"
              ECR_REPOSITORY="${ECR_EXTERNAL_REPOSITORY}"
              ;;
            *)
              echo "::error::Unsupported component: ${{ matrix.component }}"
              exit 1
              ;;
          esac

          test -n "${ECR_REPOSITORY}" || {
            echo "::error::No ECR repository is configured for ${{ matrix.component }}."
            exit 1
          }

          EXPECTED_ROLE_PREFIX="arn:aws:iam::${EXPECTED_AWS_ACCOUNT_ID}:role/"
          [[ "${AWS_ROLE_ARN}" == "${EXPECTED_ROLE_PREFIX}"* ]] || {
            echo "::error::AWS_OIDC_ROLE_ARN must reference account ${EXPECTED_AWS_ACCOUNT_ID}."
            exit 1
          }

          {
            echo "dockerfile=${DOCKERFILE}"
            echo "ecr_repository=${ECR_REPOSITORY}"
          } >> "${GITHUB_OUTPUT}"

      - name: Build release container
        shell: bash
        run: |
          set -euo pipefail
          docker buildx build \
            --file "${{ steps.component.outputs.dockerfile }}" \
            --tag "ptl-${{ matrix.component }}:${GITHUB_SHA}" \
            --label "org.opencontainers.image.revision=${GITHUB_SHA}" \
            --label "org.opencontainers.image.source=https://github.com/${GITHUB_REPOSITORY}" \
            --load \
            .

      - name: Display OIDC claims for troubleshooting
        shell: bash
        run: |
          set -euo pipefail

          RESPONSE="$(curl --silent --show-error --fail \
            --header "Authorization: bearer ${ACTIONS_ID_TOKEN_REQUEST_TOKEN}" \
            "${ACTIONS_ID_TOKEN_REQUEST_URL}&audience=sts.amazonaws.com")"

          TOKEN="$(echo "${RESPONSE}" | jq -er '.value')"
          PAYLOAD="$(echo "${TOKEN}" | cut -d '.' -f 2)"

          TOKEN_PAYLOAD="${PAYLOAD}" python3 - <<'PY'
          import base64
          import json
          import os

          payload = os.environ["TOKEN_PAYLOAD"]
          payload += "=" * (-len(payload) % 4)
          claims = json.loads(base64.urlsafe_b64decode(payload))

          for key in [
              "aud",
              "sub",
              "repository",
              "repository_owner_id",
              "repository_id",
              "ref",
              "event_name",
              "environment",
          ]:
              print(f"{key}={claims.get(key)}")
          PY

      - name: Configure AWS credentials through OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: "arn:aws:iam::783559591987:role/github_actions_pipeline_ecr_role"
          aws-region: "eu-west-2"
          role-session-name: ptl-exr-${{ github.run_id }}-${{ matrix.component }}
          audience: sts.amazonaws.com
      

      - name: Log in to Amazon ECR
        id: ecr-login
        uses: aws-actions/amazon-ecr-login@v2.1.6
        with:
          mask-password: true

      - name: Publish immutable image tag
        id: image
        shell: bash
        env:
          ECR_REGISTRY: ${{ steps.ecr-login.outputs.registry }}
        run: |
          set -euo pipefail
          IMAGE_TAG="sha-${GITHUB_SHA::12}-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          IMAGE_URI="${ECR_REGISTRY}/${{ steps.component.outputs.ecr_repository }}:${IMAGE_TAG}"

          docker tag "ptl-${{ matrix.component }}:${GITHUB_SHA}" "${IMAGE_URI}"
          docker push "${IMAGE_URI}"

          IMAGE_DIGEST="$(aws ecr describe-images \
            --repository-name "${{ steps.component.outputs.ecr_repository }}" \
            --image-ids imageTag="${IMAGE_TAG}" \
            --query 'imageDetails[0].imageDigest' \
            --output text)"

          {
            echo "image_uri=${IMAGE_URI}"
            echo "image_digest=${IMAGE_DIGEST}"
          } >> "${GITHUB_OUTPUT}"

      - name: Write publishing summary
        shell: bash
        run: |
          {
            echo "### PT-LIMS ${{ matrix.component }} image published"
            echo
            echo "- Image: \`${{ steps.image.outputs.image_uri }}\`"
            echo "- Digest: \`${{ steps.image.outputs.image_digest }}\`"
            echo "- Commit: \`${GITHUB_SHA}\`"
          } >> "${GITHUB_STEP_SUMMARY}"

  gate:
    name: CI gate
    needs: [changes, quality, sonarcloud, container-validation, publish]
    if: always()
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      CHANGES_RESULT: ${{ needs.changes.result }}
      QUALITY_RESULT: ${{ needs.quality.result }}
      SONAR_RESULT: ${{ needs.sonarcloud.result }}
      CONTAINER_RESULT: ${{ needs.container-validation.result }}
      PUBLISH_RESULT: ${{ needs.publish.result }}
      COMPONENTS: ${{ needs.changes.outputs.components }}
      SONAR_ENABLED: ${{ vars.SONAR_ENABLED }}
      EVENT_NAME: ${{ github.event_name }}
    steps:
      - name: Enforce required results
        shell: bash
        run: |
          set -euo pipefail

          [[ "${CHANGES_RESULT}" == "success" ]] || {
            echo "::error::Change detection result: ${CHANGES_RESULT}"
            exit 1
          }

          [[ "${QUALITY_RESULT}" == "success" ]] || {
            echo "::error::Quality result: ${QUALITY_RESULT}"
            exit 1
          }

          if [[ "${SONAR_ENABLED}" == "true" && "${SONAR_RESULT}" != "success" ]]; then
            echo "::error::SonarCloud is enabled but result is ${SONAR_RESULT}."
            exit 1
          fi

          if [[ "${COMPONENTS}" != "[]" && "${CONTAINER_RESULT}" != "success" ]]; then
            echo "::error::Container validation result: ${CONTAINER_RESULT}"
            exit 1
          fi

          if [[ "${EVENT_NAME}" == "push" && "${COMPONENTS}" != "[]" && "${PUBLISH_RESULT}" != "success" ]]; then
            echo "::error::Image publishing result: ${PUBLISH_RESULT}"
            exit 1
          fi

          echo "All required CI checks passed."