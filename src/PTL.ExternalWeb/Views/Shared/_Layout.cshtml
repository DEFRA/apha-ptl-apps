<!DOCTYPE html>
<html lang="en" class="govuk-template app-html-class">

<head>
    <meta charset="utf-8">
    <title>GOV.UK - Customised page template</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
    <meta name="theme-color" content="blue">
    <link rel="stylesheet" href="/css/govuk-frontend-6.5.0.min.css">
    <link rel="icon" sizes="48x48" href="/assets/images/favicon.ico">
    <link rel="icon" sizes="any" href="/assets/images/favicon.svg" type="image/svg+xml">
    <link rel="mask-icon" href="/assets/images/govuk-icon-mask.svg" color="blue">
    <link rel="apple-touch-icon" href="/assets/images/govuk-icon-180.png">
    <link rel="manifest" href="/assets/manifest.json">
    <meta property="og:image" content="YOUR-DOMAIN/images/govuk-opengraph-image.png">
    <style>
        /* Sign out must be a form post for CSRF protection; reset button chrome so it reads as a service navigation link */
        .app-signout-form { display: inline; margin: 0; }
        .app-signout-button { background: none; border: 0; padding: 0; font: inherit; cursor: pointer; }
        .govuk-header__container { min-height: 56px; display: flex; align-items: center; }
        .app-service-bar { background: #ffffff; border-bottom: 1px solid #b1b4b6; }
        .app-service-bar__container { min-height: 70px; display: flex; align-items: center; }
        .app-account-panel { margin-left: auto; display: flex; align-items: center; gap: 1rem; }
    </style>
</head>

<body class="govuk-template__body app-body-class" data-test="My value" data-other="report:details">
    <script>
            document.body.className += ' js-enabled' + ('noModule' in HTMLScriptElement.prototype ? ' govuk-frontend-supported' : '');
    </script>
    @* <form action="/form-handler" method="post" novalidate>
        <section class="govuk-cookie-banner" data-nosnippet aria-label="Cookies on Proficiency Testing">
            <div class="govuk-cookie-banner__message govuk-width-container">
                <div class="govuk-grid-row">
                    <div class="govuk-grid-column-two-thirds">
                        <h2 class="govuk-cookie-banner__heading govuk-heading-m">
                            Cookies on Proficiency Testing
                        </h2>
                        <div class="govuk-cookie-banner__content">
                            <p class="govuk-body">We use some essential cookies to make this service work.</p>
                            <p class="govuk-body">We’d also like to use analytics cookies so we can understand how you use the service and make improvements.</p>
                        </div>
                    </div>
                </div>
                <div class="govuk-button-group">
                    <button type="submit" value="yes" name="cookies[analytics]" class="govuk-button" data-module="govuk-button">
                        Accept analytics cookies
                    </button>
                    <button type="submit" value="no" name="cookies[analytics]" class="govuk-button" data-module="govuk-button">
                        Reject analytics cookies
                    </button>
                    <a class="govuk-link" href="#">View cookies</a>
                </div>
            </div>
        </section>
    </form> *@
    <a href="#main-content" class="govuk-skip-link" data-module="govuk-skip-link">Skip to main content</a>
    @model dynamic

@{
    var currentController = (ViewContext.RouteData.Values["controller"]?.ToString() ?? string.Empty).Trim();
    var currentAction = (ViewContext.RouteData.Values["action"]?.ToString() ?? string.Empty).Trim();
    var isLoginPage = string.Equals(currentController, "Account", StringComparison.OrdinalIgnoreCase)
        && string.Equals(currentAction, "Login", StringComparison.OrdinalIgnoreCase);
}

<header class="govuk-template__header">
    <div class="govuk-header">
        <div class="govuk-header__container govuk-width-container">
            <div class="govuk-header__content">
                <span class="govuk-header__link govuk-header__service-name">Proficiency Testing - External</span>
            </div>
        </div>
    </div>
<section aria-label="Service information" class="app-service-bar">
    <div class="govuk-width-container app-service-bar__container">
        <div>
            <a asp-controller="Home" asp-action="Index" aria-label="Home">
                    <img src="~/assets/images/AphaLogo.png" alt="APHA" style="height:40px; width:auto;" />
            </a>
        </div>
        @if (User.Identity is not null && User.Identity.IsAuthenticated)
        {
            <div class="app-account-panel">
                <span class="govuk-body govuk-!-margin-bottom-0">Signed in as: <strong>@User.Identity.Name</strong></span>
                <form asp-controller="Account" asp-action="Logout" method="post" class="app-signout-form">
                    @Html.AntiForgeryToken()
                    <button type="submit" class="app-signout-button govuk-link govuk-!-margin-bottom-0">Sign out</button>
                </form>
            </div>
        }
    </div>
</section>
<div class="govuk-phase-banner govuk-width-container">
    <p class="govuk-phase-banner__content">
        <strong class="govuk-tag govuk-phase-banner__content__tag">
            Alpha
        </strong>
        <span class="govuk-phase-banner__text">
            This is a new service – your <a class="govuk-link" href="#">feedback</a> will help us to improve it.
        </span>
    </p>
</div>
</header>
<div class="govuk-width-container app-width-container">
    <main role="main" class="pb-3">
        @if (!isLoginPage)
        {
            @await Html.PartialAsync("_Breadcrumbs")
        }
        @RenderBody()
    </main>
</div>
@await Html.PartialAsync("_Footer", "#")
<script type="module" src="/js/govuk-frontend-6.5.0.min.js"></script>
<script type="module">
        import {
            initAll
        } from '/js/govuk-frontend-6.5.0.min.js'
        initAll()
</script>
@await RenderSectionAsync("Scripts", required: false)
</body>

</html>
